{
  "dependencies": {
    "lit": "^2.8.0"
  },
  "devDependencies": {
    "@web/dev-server": "^0.4.0",
    "typescript": "^5.2.2"
  },
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "start": "npx web-dev-server --node-resolve --watch --open"
  }
}
